---

---

<section id="proyectos" class="proyectos">
  <div class="proyectos-container">
    <div class="proyectos-header">
      <h2 class="section-title">Proyectos</h2>
      <div class="header-line"></div>
    </div>

    <div class="projects-pagination">
      <div class="pagination-line">
        <div class="pagination-progress"></div>
      </div>
      <div class="pagination-numbers">
        <span class="current">01</span>
        <span class="separator">/</span>
        <span class="total">04</span>
      </div>
    </div>

    <div class="projects-pin-wrapper">
      <div class="projects-stack">
        <!-- Proyecto 1: Avante Street -->
        <div class="project-item">
          <div class="project-info">
            <span class="project-category">Comercial</span>
            <h3 class="project-name">Avante Street</h3>
            <p class="project-detail">
              Plaza de 3 niveles con drive-thru + roof pádel, diseñada para
              capturar la demanda real generada por el Hospital Santa María
              Chapalita + la expansión residencial de capital norte.
            </p>
            <div class="project-meta">
              <span class="meta-item">51 locales</span>
              <span class="meta-item">ROI 9-11%</span>
            </div>
            <a href="/proyectos/avante-street" class="project-cta">Ver más</a>
          </div>
          <div class="project-image">
            <img src="/images/prev-1.webp" alt="Avante Street" />
            <div class="image-badge">Preventa</div>
          </div>
        </div>

        <!-- Proyecto 2: Nova Street -->
        <div class="project-item">
          <div class="project-info">
            <span class="project-category">Comercial</span>
            <h3 class="project-name">Nova Street</h3>
            <p class="project-detail">
              Plaza 3 niveles pensada para una arteria de paso obligado con
              terrazas; captura flujo vehicular masivo y demanda de nuevos
              fraccionamientos.
            </p>
            <div class="project-meta">
              <span class="meta-item">42 locales</span>
              <span class="meta-item">ROI ~10%</span>
            </div>
            <a href="/proyectos/nova-street" class="project-cta">Ver más</a>
          </div>
          <div class="project-image">
            <img src="/images/prev-2.webp" alt="Nova Street" />
            <div class="image-badge">Preventa</div>
          </div>
        </div>

        <!-- Proyecto 3: Bosques Street -->
        <div class="project-item">
          <div class="project-info">
            <span class="project-category">Médico + Comercial</span>
            <h3 class="project-name">Bosques Street</h3>
            <p class="project-detail">
              Plaza + núcleo médico dirigida a capturar la demanda continua por
              salud y servicios en Capital Norte, con inventario optimizado para
              consultorios.
            </p>
            <div class="project-meta">
              <span class="meta-item">48 unidades</span>
              <span class="meta-item">En construcción</span>
            </div>
            <a href="/proyectos/bosques-street" class="project-cta">Ver más</a>
          </div>
          <div class="project-image">
            <img src="/plazas/Bosques Street/main.webp" alt="Bosques Street" />
            <div class="image-badge building">En construcción</div>
          </div>
        </div>

        <!-- Proyecto 4: Sendas Street -->
        <div class="project-item">
          <div class="project-info">
            <span class="project-category">Comercial</span>
            <h3 class="project-name">Sendas Street</h3>
            <p class="project-detail">
              Primera plaza funcional para una comunidad en fuerte crecimiento:
              diseñado para servicios esenciales con anclas inmediatas.
            </p>
            <div class="project-meta">
              <span class="meta-item">~35 locales</span>
              <span class="meta-item">Últimos locales</span>
            </div>
            <a href="/proyectos/sendas-street" class="project-cta">Ver más</a>
          </div>
          <div class="project-image">
            <img src="/plazas/Sendas Street/main.webp" alt="Sendas Street" />
            <div class="image-badge">Venta avanzada</div>
          </div>
        </div>
      </div>
    </div>

    <div class="proyectos-footer">
      <a href="/proyectos" class="main-cta">Explorar todos los proyectos</a>
    </div>
  </div>
</section>

<style>
  .proyectos {
    position: relative;
    width: 100%;
    background-color: #f7efe6;
    z-index: 1;
  }

  .proyectos-container {
    width: 100%;
    max-width: 1600px;
    margin: 0 auto;
    padding: 10vh 5vw;
    display: flex;
    flex-direction: column;
    height: 100vh;
    height: 100svh;
  }

  .proyectos-header {
    margin-bottom: 4vh;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    position: relative;
    z-index: 10;
    flex-shrink: 0;
  }

  .section-title {
    font-family: "Monument Extended", sans-serif;
    font-size: clamp(2rem, 6vw, 8rem);
    color: #15152a;
    font-weight: 800;
    line-height: 0.85;
    text-transform: uppercase;
    margin-bottom: 15px;
    letter-spacing: -0.04em;
  }

  .header-line {
    width: 0;
    height: 4px;
    background-color: #15152a;
    margin: 0 auto;
  }

  .projects-pin-wrapper {
    position: relative;
    width: 100%;
    flex-grow: 1;
    overflow: hidden;
    min-height: 0;
  }

  .projects-pagination {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 2vh;
    padding: 0 2vw;
    flex-shrink: 0;
  }

  .pagination-line {
    flex-grow: 1;
    height: 1px;
    background: rgba(21, 21, 42, 0.1);
    position: relative;
    overflow: hidden;
  }

  .pagination-progress {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #15152a;
    transform: scaleX(0.25);
    transform-origin: left;
  }

  .pagination-numbers {
    font-family: "Neue Montreal", sans-serif;
    font-size: 0.8rem;
    color: #15152a;
    display: flex;
    gap: 5px;
    font-weight: 500;
  }

  .pagination-numbers .total {
    opacity: 0.4;
  }

  .projects-stack {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .project-item {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 5vw;
    align-items: center;
    background-color: #f7efe6;
    opacity: 0;
    visibility: hidden;
  }

  .project-item:first-child {
    opacity: 1;
    visibility: visible;
  }

  .project-info {
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 100%;
  }

  .project-category {
    font-family: "Neue Montreal", sans-serif;
    font-size: 0.7rem;
    text-transform: uppercase;
    color: #fcc419;
    display: block;
    margin-bottom: 0.5rem;
    letter-spacing: 0.1em;
    font-weight: 600;
  }

  .project-name {
    font-family: "Monument Extended", sans-serif;
    font-size: clamp(1.5rem, 3vw, 3.5rem);
    color: #15152a;
    text-transform: uppercase;
    margin-bottom: 1rem;
    line-height: 1.1;
  }

  .project-detail {
    font-family: "Neue Montreal", sans-serif;
    font-size: 0.95rem;
    color: #15152a;
    opacity: 0.8;
    max-width: 400px;
    margin-bottom: 1rem;
    line-height: 1.5;
  }

  .project-meta {
    display: flex;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .meta-item {
    font-family: "Neue Montreal", sans-serif;
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.4rem 0.8rem;
    background: rgba(21, 21, 42, 0.05);
    color: #15152a;
    border-radius: 100px;
  }

  .project-cta {
    align-self: flex-start;
    background: transparent;
    border: 1px solid #15152a;
    color: #15152a;
    padding: 10px 25px;
    border-radius: 100px;
    font-family: "Neue Montreal", sans-serif;
    text-transform: uppercase;
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    cursor: pointer;
    transition: all 0.4s ease;
    text-decoration: none;
    display: inline-block;
  }

  .project-cta:hover {
    background: #15152a;
    color: #f7efe6;
  }

  .project-image {
    width: 100%;
    height: 100%;
    max-height: 50vh;
    overflow: hidden;
    position: relative;
    border-radius: 8px;
  }

  .project-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .image-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    font-family: "Neue Montreal", sans-serif;
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.5rem 1rem;
    background: #fcc419;
    color: #15152a;
    border-radius: 100px;
    font-weight: 600;
  }

  .image-badge.active {
    background: #4caf50;
    color: white;
  }

  .image-badge.building {
    background: #2196f3;
    color: white;
  }

  .proyectos-footer {
    margin-top: 2vh;
    text-align: center;
    flex-shrink: 0;
  }

  .main-cta {
    display: inline-block;
    background: #15152a;
    border: none;
    color: #f7efe6;
    padding: 15px 35px;
    border-radius: 100px;
    font-family: "Monument Extended", sans-serif;
    text-transform: uppercase;
    font-size: 0.75rem;
    cursor: pointer;
    transition: transform 0.3s ease;
    text-decoration: none;
  }

  .main-cta:hover {
    transform: scale(1.05);
    background: #fcc419;
    color: #15152a;
  }

  /* Responsive Adjustments */
  @media (max-width: 1024px) {
    .project-item {
      grid-template-columns: 1fr;
      gap: 4vh;
      padding: 3vh 0;
    }
    .project-image {
      height: 35vh;
      max-height: 350px;
      order: -1;
    }
    .project-name {
      font-size: 2rem;
    }
    .project-detail {
      max-width: 100%;
      font-size: 0.9rem;
    }
  }

  @media (max-width: 768px) {
    .proyectos-container {
      padding: 8vh 5vw;
    }

    .proyectos-header {
      margin-bottom: 4vh;
    }

    .section-title {
      font-size: 2.5rem;
      margin-bottom: 15px;
    }

    .projects-pagination {
      margin-bottom: 3vh;
      padding: 0;
    }

    .pagination-numbers {
      font-size: 0.75rem;
    }

    .projects-pin-wrapper {
      flex-grow: 1;
      min-height: 60vh;
    }

    .project-item {
      grid-template-columns: 1fr;
      gap: 3vh;
      padding: 0;
    }

    .project-info {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
    }

    .project-image {
      height: 40vh;
      max-height: 300px;
      order: -1;
    }

    .project-category {
      font-size: 0.7rem;
      margin-bottom: 0.5rem;
    }

    .project-name {
      font-size: 1.8rem;
      margin-bottom: 1rem;
      line-height: 1.1;
    }

    .project-detail {
      font-size: 0.85rem;
      margin-bottom: 1rem;
      line-height: 1.5;
      max-width: 100%;
    }

    .project-meta {
      margin-bottom: 1.5rem;
    }

    .project-cta {
      padding: 12px 30px;
      font-size: 0.7rem;
    }

    .proyectos-footer {
      margin-top: 3vh;
    }

    .main-cta {
      padding: 15px 35px;
      font-size: 0.75rem;
      width: 100%;
      max-width: 320px;
    }
  }

  /* Aspect Ratio Adjustments */
  @media (min-aspect-ratio: 16/10) {
    .project-image {
      max-height: 45vh;
    }
  }
</style>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const projects = gsap.utils.toArray(".project-item");

  // Initial header animation
  gsap.from(".proyectos .section-title", {
    scrollTrigger: {
      trigger: ".proyectos",
      start: "top 80%",
    },
    y: 50,
    opacity: 0,
    duration: 1.2,
    ease: "power4.out",
  });

  gsap.to(".proyectos .header-line", {
    scrollTrigger: {
      trigger: ".proyectos",
      start: "top 80%",
    },
    width: "100%",
    duration: 0.8,
    ease: "power2.inOut",
  });

  // Pinning and Stacking Animation
  const totalProjects = projects.length;
  const progressLine = document.querySelector(".pagination-progress");
  const currentNum = document.querySelector(".pagination-numbers .current");
  const totalNum = document.querySelector(".pagination-numbers .total");

  if (totalNum) {
    totalNum.textContent = totalProjects.toString().padStart(2, "0");
  }

  const masterTl = gsap.timeline({
    scrollTrigger: {
      trigger: ".proyectos",
      start: "top top",
      end: `+=${totalProjects * 100}%`,
      pin: true,
      scrub: 1,
      anticipatePin: 1,
      onUpdate: (self) => {
        const progress = self.progress;
        if (progressLine) gsap.set(progressLine, { scaleX: progress });

        const activeIndex = Math.min(
          Math.floor(progress * totalProjects),
          totalProjects - 1,
        );
        if (currentNum) {
          currentNum.textContent = (activeIndex + 1)
            .toString()
            .padStart(2, "0");
        }
      },
    },
  });

  projects.forEach((project: any, i) => {
    if (i === 0) return;

    masterTl.to(
      project,
      {
        autoAlpha: 1,
        duration: 1,
        ease: "power2.inOut",
      },
      i,
    );

    masterTl.fromTo(
      project.querySelectorAll(
        ".project-name, .project-detail, .project-cta, .project-meta",
      ),
      {
        y: 30,
        opacity: 0,
      },
      {
        y: 0,
        opacity: 1,
        stagger: 0.1,
        duration: 0.8,
      },
      i + 0.2,
    );
  });

  masterTl.from(".proyectos-footer", {
    opacity: 0,
    y: 20,
    duration: 0.5,
  });
</script>
